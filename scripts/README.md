# Scripts

- `bootstrap.sh` / `bootstrap.ps1`  
  Bootstraps the .NET solution and projects (idempotent).

- `seed.sh`  
  Creates Kafka topics and enables TimescaleDB extensions.

These scripts are safe to run multiple times.
